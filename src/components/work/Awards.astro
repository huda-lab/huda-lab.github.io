---
const { list } = Astro.props;
---

<div class="awards">
  {list.map((a) => <div class="award">🏆 {a}</div>)}
</div>

<style lang="scss">
  .awards {
    .award {
      background-color: rgb(242, 233, 195);
      color: var(--c-text);
      white-space: nowrap;
      display: inline;
      padding: 0.2rem 0.5rem;
      border-radius: 0.2rem;
      font-size: 80%;
    }
  }
</style>
