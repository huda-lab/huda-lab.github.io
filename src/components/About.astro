---
import areas from "../data/areas.json";
---

<div id="about">
  <h2>What are we about?</h2>
  <p>
    HuDa in arabic means "guidance". Our research lives at the intersection of
    data management and interactive systems. We study how we, humans, interact
    with data and we build tools that help us derive us value and insights from
    data more effectively.
    <p></p>
    <p>
      Our areas of focus change as researchers join or graduate from our lab.
      Currently, we are working on these three areas:
      <ul id="areas">
        {
          Object.entries(areas).map(([id, val]) => (
            <li class="area">
              <div class="title" style={{ "background-color": val.color }}>{val.title}</div><div class="description">{val.description}</div>
            </li>
          ))
        }
      </ul>
    </p>

    <style lang="scss">
      #about {
        margin: auto;
        padding: var(--section-padding);
        max-width: var(--s-content-width);

        #areas {
          list-style-type: none;
          padding-left: 0;

          @media (min-width: 40rem) {
            padding-left: 1rem;
          }

          .area {
            > div {
              display: inline;
            }

            .title {
              margin-right: 0.5rem;
              border-radius: 0.2rem;
              color: white;
              padding: 0.2rem 0.5rem;
              white-space: nowrap;
              overflow: hidden;
              text-transform: uppercase;
            }
          }
        }
      }
    </style>
  </p>
</div>
